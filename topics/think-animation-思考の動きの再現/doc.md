# アイディア粗案
AときたらBする
AときたらBする、そのごCときたらDする
AときたらBする、そのごCときたらDする、さらにその後EときたらFする
AときたらBする、そのごCときたらDする、
AときたらBする、そのごCときたらDする、さらにその後GときたらHする
AときたらBする、
AときたらBする、そのごIときたらJする

というように、
ストーリーの伸び縮みを順に表現する
time.sleep(1)とかで、outputに出力し続ければ、vscodeなら擬似的にアニメーションを作れる。

これは、
if分岐によりいっぺんにソースコード上に記載し俯瞰するのとはちがい、
時系列的感覚を伴う表示方法。

# 現段階評価
-   出力としては、とても良い。  
    記法は、ただ列挙しただけになっているため、判断基準などをあらかじめ共通化しておくなどの工夫がまだ必要。
-   deepcopyも関数の中だけで使えば、おそらく、その関数のスコープを抜けた時にメモリ解放してくれるはず。

# 考察
-   ソースコードは、時系列がなく、広げるモード的思考様式  
    output.mdは、時系列で表示され、まとめるモード的思考様式  
    1つのことを2つの様式で、それらが連携した形で表示できる。  
    （ただし、広げるモード→まとめるモード　の順でしか書けないが）
    -   まずソースコードも時系列で書いて、一旦動かす。  
        その後、ソースコードを分岐に整理していく。  
        さらにその後、ソースコードの分岐ごとに検討をし、新たな分岐を増やしたり、不要な分岐を減らしたりする。  
        都度、動かして全体像を見る。

# 展望、活用
-   このtopicのフォルダ丸ごとprivateにコピーして、思考整理に使用可能。
-   if分岐のイメージは、  
    分岐ロジック自体をsenario_01nのような命名の関数で記載し、それを外側から引数でフルパス通すようなイメージ。  
    ```python
    def main():
        def senario_01n(condition: bool):
            if condition is True:
                print("pattern A")
            else:
                print("pattern B")
        for condition in [True, False]:
            senario_01n(condition)
    ```
-   vscodeのmarkdownpreview拡張機能により表示したmarkdownのプレビューも  
    time.sleep(1)によって、逐次更新された。  
    たとえば、mermaidなどを使用すれば図形を用いた簡単なアニメーションを作る可能性がある。
-   向かう先を示しておきたい。期待値を最終行に表示し、結果を最後に表示し、結果と期待値が異なれば  
    巻き戻ってやり直すようなイメージ。
-   このフォーマットは、思考をまとめたりアウトプットすることではなく、  
    文書だけでは伝わらない人に向けて伝えるための力として利用することができそう。
